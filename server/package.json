{
  "name": "polly-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "nodemon server/index.js",
    "dev:frontendhost": "vite --host",
    "build": "vite build",
    "preview": "vite preview"
  },

  "dependencies": {
    "socket.io": "^4.7.0"
  },

  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
